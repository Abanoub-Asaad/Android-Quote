<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#130f40"
    tools:context=".MainActivity">

    <Button
        android:id="@+id/nextQuote_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_marginEnd="153dp"
        android:layout_marginBottom="202dp"
        android:onClick="displayQuote"
        android:text="@string/next_quote"
        android:textColor="#130f40"
        android:textSize="18sp" />

    <TextView
        android:id="@+id/textView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/nextQuote_btn"
        android:layout_centerHorizontal="true"
        android:fontFamily="@font/adamina"
        android:padding="20dp"
        android:textAppearance="?android:textAppearanceLarge"
        android:textColor="@android:color/background_light"
        android:textSize="25sp" />


    <Button
        android:id="@+id/share_btn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/nextQuote_btn"
        android:layout_centerInParent="true"
        android:onClick="shareQuote"
        android:text="@string/share"
        android:textColor="#130f40" />


</RelativeLayout>